git clone https://github.com/CLAWS-UMICH/GEMINI.git && cd GEMINI && git checkout sam && python simpletrain.py

right now we have task name for all names of tasks, if one propmt is create task + delete task how tf do you know which task name is for which??? have to have seperate token level for each possible thing for tasks and waypoints