git clone https://github.com/CLAWS-UMICH/GEMINI.git && cd GEMINI && git checkout sam && pip install -r requirements.txt && python simpletrain.py
git clone https://github.com/CLAWS-UMICH/GEMINI.git && cd GEMINI && git checkout sam && pip install -r requirements.txt && python trainairesponse.py