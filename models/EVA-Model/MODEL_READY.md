# Model Ready

Exported from `multiintentmodel/runs/multiintent_minilm_current`.

Use this command for inference:

```bash
PYTHONPATH=multiintentmodel/src python -m multiintentmodel.predict --run-dir multiintentmodel/best_model --text "open vitals and show my oxygen time left" --json
```
